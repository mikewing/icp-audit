# TPE ICP Clarity Audit — Required Environment Variables
# Copy this file to .env and fill in the values before running locally.
# On Railway, set these as environment variables in the service settings.

ANTHROPIC_API_KEY=
SUPABASE_URL=
SUPABASE_ANON_KEY=
